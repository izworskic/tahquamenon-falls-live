{
  "name": "tahquamenon-falls-live",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live Tahquamenon Falls park intelligence tool",
  "scripts": {
    "check": "node --check public/assets/tahquamenon-falls.js && node --check public/tahquamenon-falls/data/places.js && node --check api/tahquamenon-falls.js && node --check scripts/verify-integrity.mjs",
    "vercel-build": "npm run verify && node scripts/inject-ga4.mjs",
    "verify": "npm run check && node scripts/verify-integrity.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
